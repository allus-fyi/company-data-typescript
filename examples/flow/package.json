{
  "name": "@allus-fyi/flow-example",
  "version": "1.0.0",
  "private": true,
  "description": "Runnable flow example (run a contract flow end-to-end) for the allus company-data TypeScript SDK. Its own npm sub-project — NOT part of the published SDK package.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "tsx bin/start.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@allus-fyi/company-data": "file:../.."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
